<%= form_tag(search_path, method: :post) do %>
<%= select :start_station, :railway_station_id, RailwayStation.all.collect { |s| [s.title, s.id] } %>
<%= select :end_station, :railway_station_id, RailwayStation.all.collect { |s| [s.title, s.id] } %>

<%= submit_tag "Искать" %>
<% end %>

<%= render 'searches/result' %>