<%= form_for [:admin, admin_railway_station], html: {style: "display: #{hidden ? 'none' : 'block'}"} do |f| %>
<%= render 'common/errors', resource: admin_railway_station %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit t('.submit', action: admin_railway_station.new_record? ? 'Создать' : 'Изменить') %>
  </div>
<% end %>
